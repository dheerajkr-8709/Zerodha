{
  "name": "zerodha-workspace",
  "version": "1.0.0",
  "description": "Unified workspace runner for backend, frontend, and dashboard",
  "scripts": {
    "setup-all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix dashboard",
    "seed": "node backend/seed.js",
    "backend": "npm start --prefix backend",
    "frontend": "npm start --prefix frontend",
    "dashboard": "npm start --prefix dashboard",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\" \"npm run dashboard\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
